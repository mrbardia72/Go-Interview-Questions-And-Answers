# 📌 نقشه راه طراحی API (API Design)

## 🔎 مقدمه

طراحی API فرایند ساخت **رابط‌های برنامه‌نویسی کاربردی (API)** است که به برنامه‌ها و سرویس‌های نرم‌افزاری این امکان را می‌دهد تا به‌صورت ساخت‌یافته و قابل پیش‌بینی با هم ارتباط برقرار کنند. APIها اساس بسیاری از سیستم‌های مدرن در وب، موبایل و خدمات ابری هستند.

---

## 🧠 ۱. مفاهیم پایه

### 🛠 API چیست؟

API مخفف **Application Programming Interface** است و مجموعه‌ای از قراردادها و نقاط دسترسی تعریف‌شده به برنامه‌ها اجازه می‌دهد تا با یکدیگر تعامل کنند. طراحی API به معنی تعریف این قراردادها به‌صورت روشن، قابل استفاده مجدد، امن و مقیاس‌پذیر است.

### 📦 انواع API

- **RESTful APIs**  
- **SOAP APIs**  
- **GraphQL APIs**  
- **gRPC APIs**  

هر یک از این سبک‌ها مناسب سناریوهای خاصی هستند و انتخاب درست آنها به نیاز پروژه بستگی دارد.

---

## 🌐 ۲. اصول و مفاهیم HTTP

### 🚀 آشنایی با HTTP

HTTP پروتکل اصلی برای طراحی APIها در وب است و شامل موارد زیر می‌شود:

- **روش‌های HTTP (HTTP Methods):** GET, POST, PUT, PATCH, DELETE  
- **کدهای وضعیت (Status Codes):** 200, 201, 204, 400, 404, 500 و غیره  
- **هدرها (Headers):** برای کنترل کش، امنیت و محتوا  
- **URI و پارامترها:** برای آدرس‌دهی منابع مختلف  

استفاده صحیح از این مفاهیم باعث می‌شود API شما هم‌خوانی بیشتری با استانداردهای وب داشته باشد.

---

## 🧱 ۳. طراحی منابع و ساختار URI

### 📌 انتخاب نام مناسب برای منابع

نام‌گذاری باید روشن، قابل‌فهم و مطابق با قراردادهای REST باشد:

- از افعال در مسیرها اجتناب کنید  
- از اسامی جمع برای منابع استفاده کنید (مثلاً `/users` نه `/user`)  
- از شناسه‌های معنایی استفاده کنید `/users/{userId}`  

### 📍 پارامترهای مسیر، پرس‌وجو و بدنه

- **Path Parameters:** برای مشخص کردن منبع  
- **Query Parameters:** برای فیلتر کردن، مرتب‌سازی و صفحه‌بندی  
- **Request Body:** برای ارسال داده‌های پیچیده در POST/PUT  

ساختار URI مناسب باعث می‌شود API شما هم خواناتر و هم قابل استفاده‌تر شود.

---

## ⚖️ ۴. آشنایی با روش‌های مختلف طراحی API

### 📌 سبک‌های مختلف

| سبک API | ویژگی‌ها |
|---------|-----------|
| RESTful | سبک ساده، مبتنی بر منابع |
| SOAP | استاندارد قدیمی‌تر با امنیت و قرارداد شدید |
| GraphQL | امکان درخواست داده‌های دلخواه در یک درخواست |
| gRPC | پروتکل سریع و باینری برای ارتباطات سرویس‌به‌سرویس |

با توجه به نیاز پروژه و تیم، یکی از این سبک‌ها انتخاب می‌شود.

---

## 🔁 ۵. مدیریت نسخه‌گذاری (Versioning)

نسخه‌گذاری API به حفظ سازگاری با مشتریان قبلی کمک می‌کند:

- افزودن `/v1/`, `/v2/` در URI  
- استفاده از هدر برای تعیین نسخه  
- مدیریت تغییرات بزرگ و کوچک به‌صورت مجزا  

این کار تضمین می‌کند که توسعه API بدون ایجاد اختلال برای کاربران فعلی انجام شود.

---

## 🎯 ۶. عملیات CRUD و پاسخ‌های استاندارد

### 📌 عملیات CRUD

API شما باید از عملیات پایه زیر پشتیبانی کند:

| عملیات | روش HTTP | پاسخ استاندارد |
|--------|-----------|------------------|
| ایجاد (Create) | POST | 201 Created |
| خواندن (Read) | GET | 200 OK |
| بروزرسانی (Update) | PUT/PATCH | 200 OK |
| حذف (Delete) | DELETE | 204 No Content |

### 📌 استانداردسازی پاسخ‌ها

- استفاده از **JSON** برای بدنه پاسخ  
- تعریف دقیق **ساختار خطاها**  
- ارسال پیام‌های خطای قابل فهم  

این موارد باعث می‌شوند API شما قابل پیش‌بینی‌تر و قابل استفاده‌تر شود.

---

## 🔐 ۷. احراز هویت و مجوزها

APIها معمولاً باید امنیت داشته باشند:

- **API Keys**  
- **Token Based Auth (مثلاً JWT)**  
- **OAuth 2.0**  
- **Role Based Access Control (RBAC)**  

پیاده‌سازی درست این روش‌ها باعث افزایش امنیت و جلوگیری از دسترسی غیرمجاز می‌شود.

---

## 📊 ۸. کنترل خطا و تجربه توسعه‌دهنده

### 📌 مدیریت خطا

- استفاده از ساختار **Problem Details** برای پاسخ خطا  
- استانداردسازی خروجی خطاها  
- ارائه پیام‌های خطای قابل‌فهم و عملی  

این کارها کمک می‌کنند تا کاربر API سریع‌تر خطاها را درک و برطرف کند.

---

## 📜 ۹. مستندسازی و ابزارها

مستندسازی دقیق باعث می‌شود APIها برای توسعه‌دهندگان دیگر قابل استفاده باشند:

- **Swagger / OpenAPI**  
- **Postman**  
- **ReadMe.com**  
- **Stoplight**  

این ابزارها به شما کمک می‌کنند تا API خود را به‌صورت قابل‌تبادل و خوانا مستندسازی کنید.

---

## 📦 ۱۰. سبک‌های پیشرفته و موضوعات تکمیلی

### 🔹 Pagination  
### 🔹 Rate Limiting  
### 🔹 HTTP Caching  
### 🔹 Event‑Driven APIs  
### 🔹 Webhooks و Server‑Sent Events  
### 🔹 تست و مانیتورینگ API  

یادگیری این مباحث باعث می‌شود APIهای شما برای استفاده در مقیاس بالا و در محیط‌های پیچیده آماده شوند.

---

## 📍 جمع‌بندی

نقشه‌ی راه طراحی API یک راهنمای گام‌به‌گام برای یادگیری اصول، استانداردها و بهترین شیوه‌ها در طراحی API است. یادگیری و به‌کارگیری این اصول باعث می‌شود APIهایی بسازید که **امن، قابل‌اعتماد، مقیاس‌پذیر و توسعه‌پذیر** باشند.
